# Analytics Rules — Microsoft Sentinel

Exported analytics rules (scheduled, NRT, TI, ML) as JSON/YAML, deployable via Sentinel Repositories CI/CD.

Every rule ships with a specification: hypothesis, data source, logic, validation method, tuning decision, and response action.
