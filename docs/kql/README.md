# KQL

No reference material is committed at this path. The KQL language notes live in
`docs/evidence-notes/` — `kql-fundamentals-and-surfaces.md`, `filtering-search-and-where.md`,
`project-sort-and-string-operators.md`, `summarize-and-time-filtering.md`, and
`let-union-and-join.md` — and the runnable queries live in `kql/`.
