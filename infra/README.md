# Infrastructure as Code

Deployment templates for lab resources.

Parameter files are gitignored — they carry real subscription and tenant identifiers. Commit `*.parameters.example.json` with placeholder values instead.
