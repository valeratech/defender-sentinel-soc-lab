# Playbooks

No playbook definitions are committed here. The playbook exercised in this project was
deployed from Sentinel's template gallery and measured in the tenant; the record is
`labs/19-sentinel-playbooks/` and `docs/concepts/playbook-identity-model.md`. This
directory is reserved for exported Logic App definitions and currently holds none.

The policy that governs it if any land here: callback URLs contain SAS signatures and are treated as secrets — see SANITIZATION.md section 1.
